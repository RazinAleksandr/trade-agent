---
phase: 01-instrument-layer
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - lib/strategy.py
  - tools/calculate_edge.py
  - tools/calculate_kelly.py
  - tests/test_kelly.py
autonomous: true
requirements: [INST-03, INST-04]

must_haves:
  truths:
    - "Edge calculation returns estimated_prob - market_price correctly"
    - "Kelly criterion returns 0 for no-edge or negative-edge scenarios"
    - "Kelly criterion uses quarter-Kelly (0.25) by default"
    - "Position size respects max_position_size_usdc cap"
    - "Both CLI tools accept inputs and output JSON"
  artifacts:
    - path: "lib/strategy.py"
      provides: "Kelly criterion and edge calculation pure math"
      exports: ["kelly_criterion", "calculate_edge", "calculate_position_size"]
    - path: "tools/calculate_edge.py"
      provides: "CLI tool for edge calculation (INST-03)"
    - path: "tools/calculate_kelly.py"
      provides: "CLI tool for Kelly sizing (INST-04)"
  key_links:
    - from: "tools/calculate_edge.py"
      to: "lib/strategy.py"
      via: "import calculate_edge"
      pattern: "from lib\\.strategy import"
    - from: "tools/calculate_kelly.py"
      to: "lib/strategy.py"
      via: "import kelly_criterion, calculate_position_size"
      pattern: "from lib\\.strategy import"
---

<objective>
Build the pure-math strategy module (Kelly criterion, edge calculation, position sizing) and its two CLI tool wrappers. These are stateless calculations with no API dependencies.

Purpose: Edge and Kelly sizing are core to the trading decision pipeline. They're pure functions, easily testable, and can run in parallel with Plan 02 (same wave).
Output: `lib/strategy.py`, `tools/calculate_edge.py`, `tools/calculate_kelly.py`
</objective>

<execution_context>
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-instrument-layer/01-CONTEXT.md
@.planning/phases/01-instrument-layer/01-RESEARCH.md
@.planning/phases/01-instrument-layer/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (lib/ foundation) -->

From lib/config.py:
```python
@dataclass
class Config:
    kelly_fraction: float = 0.25
    min_edge_threshold: float = 0.10
    max_position_size_usdc: float = 50.0
    max_total_exposure_usdc: float = 200.0
    # ...
def load_config(args=None) -> Config: ...
```

From lib/errors.py:
```python
EXIT_INVALID_ARG = 2
EXIT_API_ERROR = 3
def error_exit(message: str, code: str, exit_code: int = 1): ...
```

<!-- Cherry-pick source from v1 -->

From strategy.py (v1 - Kelly criterion function):
```python
def kelly_criterion(prob: float, odds_price: float, fraction: float = 0.25) -> float:
    if odds_price <= 0 or odds_price >= 1:
        return 0
    b = (1 - odds_price) / odds_price  # net odds
    q = 1 - prob
    kelly = (b * prob - q) / b
    kelly = max(0, kelly)
    return kelly * fraction
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/strategy.py and tests/test_kelly.py</name>
  <files>lib/strategy.py, tests/test_kelly.py</files>
  <read_first>
    - strategy.py (v1 -- kelly_criterion function to cherry-pick, TradeSignal already in lib/models.py)
    - lib/config.py (Config dataclass fields for defaults)
    - lib/models.py (TradeSignal already defined here -- do NOT redefine)
    - .planning/phases/01-instrument-layer/01-RESEARCH.md (Example 3: Kelly Criterion, Open Question 2: edge calculation tool takes --estimated-prob)
  </read_first>
  <behavior>
    - Test: kelly_criterion(0.60, 0.50, fraction=1.0) > 0 (positive edge)
    - Test: kelly_criterion(0.50, 0.50, fraction=1.0) == 0 (no edge)
    - Test: kelly_criterion(0.40, 0.50, fraction=1.0) == 0 (negative edge, clamped to 0)
    - Test: kelly_criterion(0.60, 0.50, fraction=0.25) == kelly_criterion(0.60, 0.50, 1.0) * 0.25 (fraction scales linearly)
    - Test: kelly_criterion(0.60, 0.0) == 0 (boundary: zero price)
    - Test: kelly_criterion(0.60, 1.0) == 0 (boundary: price at 1)
    - Test: calculate_edge(0.65, 0.50) == 0.15 (simple subtraction)
    - Test: calculate_edge(0.40, 0.50) == -0.10 (negative edge)
    - Test: calculate_position_size with kelly > 0 respects max_position_size cap
    - Test: calculate_position_size returns 0.0 when notional < order_min_size (5 USDC per SAFE-05)
  </behavior>
  <action>
    **lib/strategy.py**:
    - `def kelly_criterion(prob: float, odds_price: float, fraction: float = 0.25) -> float`:
      - Cherry-pick v1 logic exactly:
        - If `odds_price <= 0 or odds_price >= 1`: return 0.0
        - `b = (1 - odds_price) / odds_price` (net odds)
        - `q = 1 - prob`
        - `kelly = (b * prob - q) / b`
        - `kelly = max(0.0, kelly)` (no negative bets)
        - `return kelly * fraction`
      - Docstring explaining: prob=estimated true probability, odds_price=price per share (payout 1.0 on win), fraction=Kelly fraction for conservative sizing

    - `def calculate_edge(estimated_prob: float, market_price: float) -> float`:
      - `return round(estimated_prob - market_price, 6)`
      - Simple subtraction. Positive = underpriced (buy opportunity). Negative = overpriced.

    - `def calculate_position_size(prob: float, price: float, bankroll: float, kelly_fraction: float = 0.25, max_position_usdc: float = 50.0, order_min_size: float = 5.0) -> dict`:
      - Calculate `kelly_raw = kelly_criterion(prob, price, fraction=1.0)`
      - Calculate `kelly_adjusted = kelly_criterion(prob, price, fraction=kelly_fraction)`
      - If `kelly_adjusted <= 0`: return `{"size_usdc": 0.0, "num_shares": 0.0, "kelly_raw": kelly_raw, "kelly_adjusted": 0.0}`
      - `position_usdc = min(kelly_adjusted * bankroll, max_position_usdc, bankroll)`
      - If `position_usdc < order_min_size`: return with `size_usdc: 0.0` (SAFE-05: below 5 USDC minimum)
      - `num_shares = round(position_usdc / price, 2)` (2 decimal places per SAFE-05)
      - Return dict: `{"size_usdc": round(position_usdc, 2), "num_shares": num_shares, "kelly_raw": round(kelly_raw, 6), "kelly_adjusted": round(kelly_adjusted, 6)}`

    **tests/test_kelly.py**:
    - Write tests FIRST (TDD). All tests listed in `<behavior>` above.
    - Additional tests:
      - `test_kelly_quarter_default()`: verify default fraction is 0.25
      - `test_position_size_capped()`: bankroll=1000, kelly says bet 200, but max_position=50, assert size_usdc=50
      - `test_position_size_below_minimum()`: scenario where kelly * bankroll < 5.0, assert size_usdc=0.0
      - `test_position_size_shares_rounded()`: verify num_shares has at most 2 decimal places
      - `test_edge_positive()`: `calculate_edge(0.65, 0.50)` returns 0.15
      - `test_edge_negative()`: `calculate_edge(0.40, 0.50)` returns -0.10
      - `test_edge_zero()`: `calculate_edge(0.50, 0.50)` returns 0.0
  </action>
  <verify>
    <automated>cd /Users/aleksandrrazin/work/polymarket-agent && source .venv/bin/activate && python -m pytest tests/test_kelly.py -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - `lib/strategy.py` contains `def kelly_criterion(`, `def calculate_edge(`, `def calculate_position_size(`
    - `lib/strategy.py` kelly_criterion has `max(0.0, kelly)` (no negative bets)
    - `lib/strategy.py` kelly_criterion default fraction is 0.25
    - `lib/strategy.py` calculate_position_size enforces `order_min_size` (5 USDC minimum)
    - `lib/strategy.py` calculate_position_size rounds num_shares to 2 decimal places
    - `tests/test_kelly.py` has at least 10 test functions
    - `python -m pytest tests/test_kelly.py -x` exits 0
  </acceptance_criteria>
  <done>Kelly criterion, edge calculation, and position sizing are implemented with correct math, boundary conditions handled, minimum order size enforced, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create tools/calculate_edge.py and tools/calculate_kelly.py CLI wrappers</name>
  <files>tools/calculate_edge.py, tools/calculate_kelly.py</files>
  <read_first>
    - lib/strategy.py (just created -- function signatures)
    - lib/config.py (load_config for defaults)
    - lib/errors.py (error_exit, exit codes)
    - tools/discover_markets.py (reference CLI pattern if Plan 02 completed first, otherwise use RESEARCH.md Pattern 1)
    - .planning/phases/01-instrument-layer/01-RESEARCH.md (Pattern 1: CLI Tool Structure)
  </read_first>
  <action>
    **tools/calculate_edge.py** (per INST-03, D-01 through D-04, Open Question 2 from RESEARCH):
    - Shebang: `#!/usr/bin/env python3`
    - Docstring: `"""Calculate edge for a market given estimated probability and market price."""`
    - argparse arguments:
      - `--estimated-prob` type=float, required=True, help="Estimated true probability (0.0-1.0)" (dest="estimated_prob")
      - `--market-price` type=float, required=True, help="Current market price (0.0-1.0)" (dest="market_price")
      - `--pretty` action="store_true"
    - Validate: 0 < estimated_prob < 1 and 0 < market_price < 1. If not, `error_exit("Probability and price must be between 0 and 1 exclusive", "INVALID_ARG", EXIT_INVALID_ARG)`
    - Call `calculate_edge(args.estimated_prob, args.market_price)`
    - Output JSON to stdout: `{"estimated_prob": ..., "market_price": ..., "edge": ..., "edge_pct": f"{edge*100:.2f}%", "direction": "BUY_YES" if edge > 0 else "BUY_NO" if edge < 0 else "NO_EDGE"}`
    - `if __name__ == "__main__": main()`

    **tools/calculate_kelly.py** (per INST-04, D-01 through D-04, D-08):
    - Shebang: `#!/usr/bin/env python3`
    - Docstring: `"""Calculate Kelly criterion position size for a trade."""`
    - argparse arguments:
      - `--estimated-prob` type=float, required=True, help="Estimated true probability (0.0-1.0)" (dest="estimated_prob")
      - `--market-price` type=float, required=True, help="Current market price / price per share (0.0-1.0)" (dest="market_price")
      - `--bankroll` type=float, default=None, help="Available bankroll in USDC (default: MAX_TOTAL_EXPOSURE_USDC from .env)"
      - `--kelly-fraction` type=float, default=None, help="Kelly fraction (default: KELLY_FRACTION from .env)" (dest="kelly_fraction")
      - `--max-position` type=float, default=None, help="Max position size in USDC" (dest="max_position_size_usdc")
      - `--pretty` action="store_true"
    - `config = load_config(args)`
    - Validate prob and price in (0, 1) range
    - `bankroll = args.bankroll if args.bankroll is not None else config.max_total_exposure_usdc`
    - Call `calculate_position_size(prob=args.estimated_prob, price=args.market_price, bankroll=bankroll, kelly_fraction=config.kelly_fraction, max_position_usdc=config.max_position_size_usdc)`
    - Also compute edge via `calculate_edge(args.estimated_prob, args.market_price)`
    - Output JSON to stdout: merge position_size result with `{"estimated_prob": ..., "market_price": ..., "edge": ..., "bankroll": bankroll}`
    - `if __name__ == "__main__": main()`
  </action>
  <verify>
    <automated>cd /Users/aleksandrrazin/work/polymarket-agent && source .venv/bin/activate && python tools/calculate_edge.py --estimated-prob 0.65 --market-price 0.50 && echo "---" && python tools/calculate_kelly.py --estimated-prob 0.65 --market-price 0.50 --bankroll 200</automated>
  </verify>
  <acceptance_criteria>
    - `tools/calculate_edge.py` has `--estimated-prob` and `--market-price` as required args
    - `tools/calculate_edge.py` output JSON contains keys: `estimated_prob`, `market_price`, `edge`, `direction`
    - `tools/calculate_edge.py` validates prob and price in (0, 1)
    - `tools/calculate_kelly.py` has `--estimated-prob`, `--market-price` as required, `--bankroll`, `--kelly-fraction`, `--max-position` as optional
    - `tools/calculate_kelly.py` output JSON contains keys: `size_usdc`, `num_shares`, `kelly_raw`, `kelly_adjusted`, `edge`
    - `python tools/calculate_edge.py --estimated-prob 0.65 --market-price 0.50` exits 0 and outputs valid JSON
    - `python tools/calculate_kelly.py --estimated-prob 0.65 --market-price 0.50 --bankroll 200` exits 0 and outputs valid JSON
    - Both tools support `--pretty` flag
    - Both tools use `error_exit` for errors (JSON to stderr)
  </acceptance_criteria>
  <done>Both edge and Kelly CLI tools work correctly, accept required params, output JSON, validate inputs, and use config defaults with CLI override support</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_kelly.py -x -v` passes
- `python tools/calculate_edge.py --estimated-prob 0.65 --market-price 0.50` outputs JSON with `edge: 0.15`
- `python tools/calculate_kelly.py --estimated-prob 0.65 --market-price 0.50 --bankroll 200` outputs JSON with positive size_usdc
- `python tools/calculate_edge.py --help` shows usage
- `python tools/calculate_kelly.py --help` shows usage
</verification>

<success_criteria>
- Kelly criterion math matches v1 exactly (quarter Kelly default, clamped at 0)
- Edge calculation is simple prob - price
- Position sizing respects max cap and 5 USDC minimum
- Both CLI tools output JSON, validate inputs, support --help and --pretty
- All 10+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrument-layer/01-03-SUMMARY.md`
</output>
