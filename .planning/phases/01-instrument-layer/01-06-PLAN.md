---
phase: 01-instrument-layer
plan: 06
type: execute
wave: 4
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - main.py
  - config.py
  - market_discovery.py
  - market_analyzer.py
  - strategy.py
  - trader.py
  - portfolio.py
  - data_store.py
  - logger_setup.py
  - .env.example
  - .gitignore
autonomous: false
requirements: [INST-01, INST-02, INST-03, INST-04, INST-05, INST-06, INST-07, INST-08, INST-09, INST-10, INST-11, INST-12, INST-13]

must_haves:
  truths:
    - "All v1 root .py files are deleted (per D-06)"
    - "No imports reference v1 root modules"
    - "All 7 CLI tools run with --help"
    - ".env.example documents all required and optional env vars"
    - "Full test suite passes"
    - "Running discover_markets.py against live Gamma API returns market data"
  artifacts:
    - path: ".env.example"
      provides: "Documentation of all env vars"
    - path: ".gitignore"
      provides: "Updated ignore patterns for new structure"
  key_links:
    - from: "tools/*.py"
      to: "lib/*.py"
      via: "All tool imports reference lib/ package"
      pattern: "from lib\\."
---

<objective>
Delete v1 legacy files (per D-06), create .env.example, update .gitignore, run the full integration test suite, and verify all 7 CLI tools work end-to-end. This is the final cleanup and verification plan.

Purpose: Clean break from v1 structure. Verify the complete instrument layer works as an integrated whole.
Output: Clean repo with only `tools/` + `lib/` + `tests/` structure, .env.example, passing full test suite
</objective>

<execution_context>
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-instrument-layer/01-CONTEXT.md
@.planning/phases/01-instrument-layer/01-01-SUMMARY.md
@.planning/phases/01-instrument-layer/01-02-SUMMARY.md
@.planning/phases/01-instrument-layer/01-03-SUMMARY.md
@.planning/phases/01-instrument-layer/01-04-SUMMARY.md
@.planning/phases/01-instrument-layer/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete v1 legacy files, create .env.example, update .gitignore, run full test suite</name>
  <files>main.py, config.py, market_discovery.py, market_analyzer.py, strategy.py, trader.py, portfolio.py, data_store.py, logger_setup.py, .env.example, .gitignore</files>
  <read_first>
    - .gitignore (current gitignore to update, not replace)
    - main.py (confirm this is v1 file to delete -- should have autonomous loop, not tool pattern)
    - config.py (confirm this is v1 flat config, not lib/config.py)
    - tests/test_paper_trading.py (v1 test file -- may need deletion or adaptation)
  </read_first>
  <action>
    **Step 1: Delete v1 root .py files (per D-06)**
    Remove these files (all v1 legacy, replaced by lib/ + tools/):
    - `main.py` (v1 autonomous loop -- replaced by agent layer in Phase 2)
    - `config.py` (replaced by `lib/config.py`)
    - `market_discovery.py` (replaced by `lib/market_data.py`)
    - `market_analyzer.py` (OpenAI analysis -- not needed in Phase 1, deferred to Phase 2)
    - `strategy.py` (replaced by `lib/strategy.py`)
    - `trader.py` (replaced by `lib/trading.py`)
    - `portfolio.py` (replaced by `lib/portfolio.py`)
    - `data_store.py` (replaced by `lib/db.py`)
    - `logger_setup.py` (replaced by `lib/logging_setup.py`)

    Do NOT delete: `setup_wallet.py` (still needed for live trading wallet setup, referenced by Phase 4)
    Do NOT delete: `tests/test_paper_trading.py` -- instead rename or delete it since tests/ now has new pytest-based tests. Delete it if all its coverage is now in the new test files. If any tests are unique, adapt them.

    **Step 2: Create .env.example** (per INST-11)
    Create `.env.example` with ALL configurable parameters, comments, and defaults:
    ```
    # Polymarket Autonomous Trading Agent Configuration
    # Copy to .env and fill in required values

    # === REQUIRED ===
    # PRIVATE_KEY=your_ethereum_private_key  # Only needed for live trading

    # === TRADING MODE ===
    PAPER_TRADING=true  # Set to "false" for live trading (requires PRIVATE_KEY)

    # === RISK PARAMETERS ===
    MIN_EDGE_THRESHOLD=0.10      # Minimum edge to consider a trade (10%)
    KELLY_FRACTION=0.25          # Quarter Kelly for conservative sizing
    MAX_POSITION_SIZE_USDC=50    # Maximum single position size in USDC
    MAX_TOTAL_EXPOSURE_USDC=200  # Maximum total portfolio exposure in USDC

    # === MARKET DISCOVERY FILTERS ===
    MIN_VOLUME_24H=1000          # Minimum 24h volume in USDC
    MIN_LIQUIDITY=500            # Minimum liquidity in USDC
    MAX_MARKETS_PER_CYCLE=10     # Maximum markets to return per discovery

    # === API ENDPOINTS ===
    POLYMARKET_HOST=https://clob.polymarket.com
    GAMMA_API_URL=https://gamma-api.polymarket.com
    CHAIN_ID=137                 # Polygon mainnet

    # === DATABASE ===
    DB_PATH=trading.db

    # === LOGGING ===
    LOG_LEVEL=INFO               # DEBUG, INFO, WARNING, ERROR
    LOG_FILE=trading.log         # Structured JSON log file
    ```

    **Step 3: Update .gitignore**
    Ensure these entries exist (add if missing, don't remove existing entries):
    ```
    .env
    trading.db
    trading.log
    __pycache__/
    *.pyc
    .venv/
    .claude/
    ```

    **Step 4: Verify no dangling imports**
    Run a grep to confirm no file in `lib/` or `tools/` imports from v1 root modules:
    - Should NOT find: `from config import`, `from data_store import`, `from logger_setup import`, `from market_discovery import`, `from strategy import`, `from trader import`, `from portfolio import`
    - Should find: `from lib.config import`, `from lib.db import`, `from lib.logging_setup import`, etc.

    **Step 5: Run full test suite**
    ```bash
    python -m pytest tests/ -v --timeout=30
    ```
    All tests must pass.

    **Step 6: Verify all 7 CLI tools run with --help**
    ```bash
    python tools/discover_markets.py --help
    python tools/get_prices.py --help
    python tools/calculate_edge.py --help
    python tools/calculate_kelly.py --help
    python tools/execute_trade.py --help
    python tools/get_portfolio.py --help
    python tools/check_resolved.py --help
    ```
    All must exit 0.
  </action>
  <verify>
    <automated>cd /Users/aleksandrrazin/work/polymarket-agent && source .venv/bin/activate && python -m pytest tests/ -v --timeout=30 && python tools/discover_markets.py --help > /dev/null && python tools/get_prices.py --help > /dev/null && python tools/calculate_edge.py --help > /dev/null && python tools/calculate_kelly.py --help > /dev/null && python tools/execute_trade.py --help > /dev/null && python tools/get_portfolio.py --help > /dev/null && python tools/check_resolved.py --help > /dev/null && echo "ALL TOOLS OK"</automated>
  </verify>
  <acceptance_criteria>
    - Files deleted: `main.py`, `config.py`, `market_discovery.py`, `market_analyzer.py`, `strategy.py`, `trader.py`, `portfolio.py`, `data_store.py`, `logger_setup.py` do NOT exist in project root
    - `setup_wallet.py` still exists in project root
    - `.env.example` exists and contains all 15 config parameters with comments
    - `.gitignore` contains entries for `.env`, `trading.db`, `trading.log`, `__pycache__/`, `.venv/`
    - No file in `lib/` or `tools/` contains `from config import` or `from data_store import` or `from logger_setup import` (without `lib.` prefix)
    - `python -m pytest tests/ -v --timeout=30` exits 0 with all tests passing
    - All 7 `python tools/*.py --help` commands exit 0
  </acceptance_criteria>
  <done>V1 legacy files deleted, .env.example created, .gitignore updated, no dangling imports, full test suite passes, all 7 CLI tools verified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete instrument layer</name>
  <files>.env.example</files>
  <action>
    Present the complete instrument layer for human verification. All automation is complete in Task 1 -- this checkpoint verifies the integrated result by asking the user to run specific commands and confirm the output matches expectations.
  </action>
  <what-built>
    Complete instrument layer: 7 CLI tools in tools/, shared lib/ package, full test suite. V1 legacy files removed. All tools output JSON to stdout, errors to stderr, support --help and --pretty.

    Tools created:
    1. tools/discover_markets.py -- Market discovery from Gamma API (INST-01)
    2. tools/get_prices.py -- Orderbook prices from CLOB API (INST-02)
    3. tools/calculate_edge.py -- Edge calculation (INST-03)
    4. tools/calculate_kelly.py -- Kelly criterion sizing (INST-04)
    5. tools/execute_trade.py -- Paper and live trade execution (INST-05/06)
    6. tools/get_portfolio.py -- Portfolio with P&L (INST-07)
    7. tools/check_resolved.py -- Resolved market detection (INST-08)
  </what-built>
  <how-to-verify>
    1. Run the full test suite:
       ```
       source .venv/bin/activate
       python -m pytest tests/ -v
       ```
       Expected: All tests pass.

    2. Test market discovery against live API:
       ```
       python tools/discover_markets.py --limit 3 --pretty
       ```
       Expected: JSON array of 1-3 markets with fields: id, question, yes_price, neg_risk, volume_24h, etc.

    3. Test edge calculation:
       ```
       python tools/calculate_edge.py --estimated-prob 0.65 --market-price 0.50 --pretty
       ```
       Expected: JSON with edge=0.15, direction="BUY_YES"

    4. Test Kelly sizing:
       ```
       python tools/calculate_kelly.py --estimated-prob 0.65 --market-price 0.50 --bankroll 200 --pretty
       ```
       Expected: JSON with positive size_usdc and num_shares values

    5. Verify v1 files are gone:
       ```
       ls main.py config.py market_discovery.py 2>&1
       ```
       Expected: "No such file or directory" for each

    6. Verify .env.example exists:
       ```
       head -5 .env.example
       ```
       Expected: Comment header and config section
  </how-to-verify>
  <verify>Human confirms all 6 verification steps produce expected output</verify>
  <done>User approves the complete instrument layer</done>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Full test suite: `python -m pytest tests/ -v --timeout=30` all pass
- All 7 tools: `python tools/{name}.py --help` all exit 0
- No v1 root .py files remain (except setup_wallet.py)
- .env.example documents all parameters
- Live Gamma API test: `python tools/discover_markets.py --limit 2` returns markets
</verification>

<success_criteria>
- Clean repo with tools/ + lib/ + tests/ structure (v1 root files deleted)
- All 13 INST requirements addressed by tool implementations
- Full test suite passing
- .env.example complete
- Human verification of end-to-end tool functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-instrument-layer/01-06-SUMMARY.md`
</output>
