---
phase: 03-strategy-evolution
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - .claude/agents/trading-cycle.md
autonomous: true
requirements: [STRT-01, STRT-02, STRT-04, STRT-05, STRT-06, STRT-07]

must_haves:
  truths:
    - "trading-cycle.md Step 0 reads both state/strategy.md and state/core-principles.md at cycle start"
    - "trading-cycle.md has a Step 7 that spawns the Strategy Updater sub-agent after the Reviewer"
    - "trading-cycle.md Step 7 validates strategy_update.json output from the Updater"
    - "Strategy Updater failure does NOT block cycle completion"
    - "The 'NEVER modify strategy.md' constraint is removed from trading-cycle.md"
    - "The old Step 7 (Cycle Completion) becomes Step 8"
  artifacts:
    - path: ".claude/agents/trading-cycle.md"
      provides: "Extended trading pipeline with Strategy Updater step"
      contains: "## Step 7: Strategy Update"
  key_links:
    - from: ".claude/agents/trading-cycle.md"
      to: ".claude/agents/strategy-updater.md"
      via: "Task tool spawn in Step 7"
      pattern: "strategy-updater"
    - from: ".claude/agents/trading-cycle.md"
      to: "state/core-principles.md"
      via: "Read in Step 0"
      pattern: "core-principles\\.md"
    - from: ".claude/agents/trading-cycle.md"
      to: "state/cycles/{cycle_id}/strategy_update.json"
      via: "Read and validate after Step 7"
      pattern: "strategy_update\\.json"
---

<objective>
Extend the main trading-cycle orchestration agent to integrate the Strategy Updater as Step 7, read core-principles.md at cycle start, and remove the "NEVER modify strategy.md" constraint.

Purpose: Closes the strategy evolution feedback loop by wiring the Strategy Updater sub-agent into the pipeline. After this plan, a complete trading cycle will automatically update the strategy document based on Reviewer analysis.

Output: Modified trading-cycle.md with Step 0 (reads core-principles.md), Step 7 (Strategy Update), and Step 8 (Cycle Completion).
</objective>

<execution_context>
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-strategy-evolution/03-CONTEXT.md
@.planning/phases/03-strategy-evolution/03-RESEARCH.md
@.planning/phases/03-strategy-evolution/03-01-SUMMARY.md

<interfaces>
<!-- The Strategy Updater agent created in Plan 01 that this plan spawns -->

From .claude/agents/strategy-updater.md (created in Plan 01):
```yaml
---
name: strategy-updater
description: Updates state/strategy.md based on Reviewer learnings and suggestions. Writes structured audit output. Commits strategy changes via git.
tools: Bash, Read, Write
model: inherit
maxTurns: 8
permissionMode: bypassPermissions
---
```
Outputs: state/cycles/{cycle_id}/strategy_update.json
Required JSON fields: cycle_id, timestamp, changes_applied, changes, deferred, summary, git_committed

From .claude/agents/trading-cycle.md (current structure to be modified):
- Step 0: Cycle Initialization (reads strategy.md + 3 recent reports)
- Step 1: Scanner
- Step 2: Analyst
- Step 3: Risk Manager
- Step 4: Planner
- Step 5: Execute Trades
- Step 6: Reviewer
- Step 7: Cycle Completion (log summary)
- Constraints section includes: "NEVER modify state/strategy.md"

From lib/agent_schemas.py (validation function created in Plan 01):
```python
def validate_strategy_update(data: dict) -> tuple[bool, str]:
    # Validates: cycle_id, timestamp, changes_applied, changes, deferred, summary, git_committed
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend trading-cycle.md with Strategy Updater integration</name>
  <files>
    .claude/agents/trading-cycle.md
  </files>
  <read_first>
    .claude/agents/trading-cycle.md,
    .claude/agents/strategy-updater.md,
    .planning/phases/03-strategy-evolution/03-CONTEXT.md,
    .planning/phases/03-strategy-evolution/03-RESEARCH.md
  </read_first>
  <action>
Modify `.claude/agents/trading-cycle.md` with these specific changes. The file is 242 lines. Make surgical edits to 3 areas:

**Change 1: Update Step 0 to read core-principles.md (per D-06)**

In Step 0, item 3 currently reads:
```
3. **Read the current trading strategy** from `state/strategy.md`. This document contains the evolving trading strategy with rules, learnings, and parameters. If it is empty or minimal (first cycles), note this and proceed with default rules.
```

Replace it with:
```
3. **Read the current trading strategy** from `state/strategy.md` and `state/core-principles.md`.
   - `state/strategy.md` contains the evolving trading strategy with rules organized in 4 domains (Market Selection Rules, Analysis Approach, Risk Parameters, Trade Entry/Exit Rules). If it is empty or minimal (first cycles), note this and proceed with default rules.
   - `state/core-principles.md` contains human-set principles that are immutable. Read but never modify this file. If it contains only the placeholder text ("To be defined after initial cycles"), note that principles are not yet established and proceed.
```

**Change 2: Replace Step 7 with Strategy Update, add Step 8 as Cycle Completion (per D-05)**

The current Step 7 (Cycle Completion) becomes Step 8. Insert a new Step 7 for Strategy Update.

Replace the current `## Step 7: Cycle Completion` section (and everything under it until `## Error Handling`) with:

```markdown
## Step 7: Strategy Update

Spawn the Strategy Updater sub-agent via Task tool:
- **subagent_type:** "strategy-updater"
- **prompt:**
  ```
  Update the trading strategy based on cycle {cycle_id} review.
  Read reviewer output from: state/cycles/{cycle_id}/reviewer_output.json
  Read current strategy from: state/strategy.md
  Write your update output to: state/cycles/{cycle_id}/strategy_update.json
  ```

After the Strategy Updater completes:
- Read `state/cycles/{cycle_id}/strategy_update.json`
- **Validate** the JSON has these required fields:
  - `cycle_id`, `timestamp`
  - `changes_applied` (number)
  - `changes` (array, each item with `domain`, `type`, `description`)
  - `deferred` (array)
  - `summary` (string)
  - `git_committed` (boolean)
- If the Strategy Updater fails or output is invalid: log the error but **do NOT fail the cycle**. The cycle is already complete (trades executed, report written). Strategy update is a post-cycle enhancement.
- **Log:** "Strategy updated: {changes_applied} changes applied, {len(deferred)} deferred"

## Step 8: Cycle Completion

Log a complete cycle summary:
```
Cycle {cycle_id} complete:
  Markets scanned: {N}
  Markets analyzed: {N}
  Trades executed: {N}
  Capital deployed: ${N} USDC
  Learnings: {count}
  Strategy suggestions: {count}
  Strategy changes applied: {count}
```
```

**Change 3: Update Error Handling section**

In the `## Error Handling` section, the numbered list currently has 6 items. Add item 7 and adjust the existing item 6:

After item 6 (`6. **Reviewer fails:** Log error but the cycle is still considered complete (trades were already executed).`), add:
```
7. **Strategy Updater fails:** Log error but the cycle is still considered complete (trades executed, report written). Strategy update is a post-cycle enhancement -- failure here is non-blocking.
```

**Change 4: Update Constraints section**

In the `## Constraints` section, find and REMOVE this line:
```
- **NEVER** modify `state/strategy.md` -- strategy updates are Phase 3 scope.
```

Replace it with:
```
- **NEVER** modify `state/core-principles.md` -- that file is human-operated only, read at cycle start but never written by any agent.
- `state/strategy.md` is modified ONLY by the Strategy Updater sub-agent in Step 7. The main agent and all other sub-agents must NOT write to it directly.
```

**Change 5: Update the description in YAML frontmatter**

In the YAML frontmatter, update the `description` field to mention the Strategy Updater:
```yaml
description: Runs a complete Polymarket trading cycle -- orchestrating Scanner, Analyst, Risk Manager, Planner, Reviewer, and Strategy Updater sub-agents in sequence. Reads strategy and core principles at start, executes approved trades, produces cycle report, and updates strategy.
```
  </action>
  <verify>
    <automated>cd /Users/aleksandrrazin/work/polymarket-agent && grep -q "## Step 7: Strategy Update" .claude/agents/trading-cycle.md && grep -q "## Step 8: Cycle Completion" .claude/agents/trading-cycle.md && grep -q "core-principles.md" .claude/agents/trading-cycle.md && grep -q "strategy-updater" .claude/agents/trading-cycle.md && grep -q "strategy_update.json" .claude/agents/trading-cycle.md && grep -q "Strategy Updater fails" .claude/agents/trading-cycle.md && ! grep -q "NEVER.*modify.*strategy.md.*Phase 3" .claude/agents/trading-cycle.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/trading-cycle.md contains `## Step 7: Strategy Update`
    - .claude/agents/trading-cycle.md contains `## Step 8: Cycle Completion`
    - .claude/agents/trading-cycle.md contains `state/core-principles.md` in Step 0
    - .claude/agents/trading-cycle.md contains `subagent_type:` with value `strategy-updater` in Step 7
    - .claude/agents/trading-cycle.md contains `strategy_update.json` (validation of Updater output)
    - .claude/agents/trading-cycle.md contains `Strategy Updater fails` in Error Handling section with non-blocking language
    - .claude/agents/trading-cycle.md does NOT contain `NEVER modify.*strategy.md -- strategy updates are Phase 3`
    - .claude/agents/trading-cycle.md Constraints section contains `NEVER.*modify.*core-principles.md`
    - .claude/agents/trading-cycle.md Constraints section contains `Strategy Updater sub-agent in Step 7`
    - .claude/agents/trading-cycle.md frontmatter description mentions `Strategy Updater`
    - .claude/agents/trading-cycle.md Step 0 mentions both `strategy.md` and `core-principles.md`
    - .claude/agents/trading-cycle.md Step 7 includes `changes_applied` in validation requirements
    - .claude/agents/trading-cycle.md Step 8 includes `Strategy changes applied` in cycle summary
    - `python -m pytest tests/ -x -q` exits 0 (no regressions)
  </acceptance_criteria>
  <done>
    trading-cycle.md is extended with:
    (1) Step 0 reads both strategy.md and core-principles.md
    (2) Step 7 spawns Strategy Updater, validates output, handles failures gracefully
    (3) Step 8 is the renamed Cycle Completion with strategy change count
    (4) Error Handling includes Strategy Updater failure as non-blocking
    (5) Constraints updated: core-principles.md is NEVER modified, strategy.md is only modified by Strategy Updater
    (6) Frontmatter description mentions Strategy Updater
    The "NEVER modify strategy.md" constraint is removed per D-05.
  </done>
</task>

</tasks>

<verification>
- `grep "## Step 7: Strategy Update" .claude/agents/trading-cycle.md` finds the new step
- `grep "## Step 8: Cycle Completion" .claude/agents/trading-cycle.md` finds the renamed step
- `grep "core-principles.md" .claude/agents/trading-cycle.md` confirms Step 0 reads both files
- `grep "strategy-updater" .claude/agents/trading-cycle.md` confirms Step 7 spawns the agent
- `grep -v "NEVER.*modify.*strategy.md.*Phase 3" .claude/agents/trading-cycle.md` confirms old constraint removed
- `python -m pytest tests/ -x -q` passes (no test regressions from agent definition changes)
</verification>

<success_criteria>
- trading-cycle.md has 9 steps (Step 0 through Step 8) with Strategy Update at Step 7
- Step 0 reads both state/strategy.md and state/core-principles.md
- Step 7 spawns the strategy-updater sub-agent and validates its output
- Strategy Updater failure is explicitly non-blocking (cycle already complete after Step 6)
- The old "NEVER modify strategy.md" constraint is replaced with core-principles.md protection
- Full test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-evolution/03-02-SUMMARY.md`
</output>
