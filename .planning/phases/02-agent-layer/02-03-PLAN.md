---
phase: 02-agent-layer
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .claude/agents/risk-manager.md
  - .claude/agents/planner.md
  - .claude/agents/reviewer.md
autonomous: true
requirements: [AGNT-05, AGNT-06, AGNT-07, AGNT-08, AGNT-10]

must_haves:
  truths:
    - "Risk Manager agent reads analyst outputs and portfolio state, applies Kelly sizing with confidence weighting, detects correlated positions, and writes risk_output.json"
    - "Planner agent reads all prior outputs plus strategy.md and creates a concrete trade plan with specific orders"
    - "Reviewer agent analyzes cycle results and writes both a cycle report markdown and reviewer_output.json"
    - "All three agents have maxTurns set in frontmatter"
  artifacts:
    - path: ".claude/agents/risk-manager.md"
      provides: "Risk Manager sub-agent with Kelly sizing, exposure limits, and correlation detection"
      contains: "name: risk-manager"
      min_lines: 80
    - path: ".claude/agents/planner.md"
      provides: "Planner sub-agent that synthesizes all prior outputs into a trade plan"
      contains: "name: planner"
      min_lines: 60
    - path: ".claude/agents/reviewer.md"
      provides: "Reviewer sub-agent that analyzes cycle results and writes reports"
      contains: "name: reviewer"
      min_lines: 70
  key_links:
    - from: ".claude/agents/risk-manager.md"
      to: "tools/get_portfolio.py, tools/calculate_kelly.py, tools/calculate_edge.py"
      via: "Bash tool invocations"
      pattern: "python.*tools/(get_portfolio|calculate_kelly|calculate_edge).py"
    - from: ".claude/agents/risk-manager.md"
      to: "state/cycles/{cycle_id}/analyst_*.json"
      via: "Read tool"
      pattern: "analyst.*json"
    - from: ".claude/agents/planner.md"
      to: "state/strategy.md"
      via: "Read tool"
      pattern: "state/strategy.md"
    - from: ".claude/agents/reviewer.md"
      to: "state/reports/"
      via: "Write tool"
      pattern: "state/reports/cycle-"
---

<objective>
Create the Risk Manager, Planner, and Reviewer sub-agent definition files that form the decision-making and review stages of the trading pipeline.

Purpose: The Risk Manager sizes positions with Kelly criterion and correlation detection (AGNT-05, AGNT-06). The Planner creates a concrete trade plan from all prior analysis (AGNT-07). The Reviewer evaluates cycle outcomes for strategy evolution (AGNT-08).
Output: Three `.claude/agents/*.md` files ready for Task tool invocation.
</objective>

<execution_context>
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aleksandrrazin/work/polymarket-agent/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-agent-layer/02-CONTEXT.md
@.planning/phases/02-agent-layer/02-RESEARCH.md

<interfaces>
<!-- Phase 1 CLI tools the Risk Manager will call -->
From tools/get_portfolio.py:
  Usage: python tools/get_portfolio.py [--pretty] [--include-risk]
  Output: JSON with open positions, exposure, unrealized P&L, risk warnings

From tools/calculate_edge.py:
  Usage: python tools/calculate_edge.py --estimated-prob PROB --market-price PRICE [--pretty]
  Output: JSON with edge value, side recommendation

From tools/calculate_kelly.py:
  Usage: python tools/calculate_kelly.py --estimated-prob PROB --market-price PRICE [--bankroll N] [--kelly-fraction F] [--max-position N] [--pretty]
  Output: JSON with kelly_raw, kelly_adjusted, position_size_usdc, num_shares

From tools/check_resolved.py:
  Usage: python tools/check_resolved.py [--pretty]
  Output: JSON with resolved markets and finalized P&L

From tools/execute_trade.py:
  Usage: python tools/execute_trade.py --market-id ID --token-id TID --side YES|NO --size SIZE [--price P] [--question Q] [--condition-id CID] [--estimated-prob EP] [--edge E] [--reasoning R] [--neg-risk] [--live] [--pretty]

<!-- JSON schemas from 02-RESEARCH.md -->
Risk Manager output: { cycle_id, timestamp, portfolio_state: { total_exposure, remaining_capacity, num_open_positions, utilization }, evaluated_markets: [{ market_id, question, estimated_prob, confidence, market_price, edge, kelly_raw, kelly_adjusted, recommended_side, position_size_usdc, num_shares, approved, correlation_flag, sizing_notes }], rejected_markets: [{ market_id, reason, edge, min_threshold }], risk_warnings: [] }

Trade Plan output: { cycle_id, timestamp, strategy_context, trades: [{ market_id, question, action, side, token_id, size, price, cost_usdc, edge, estimated_prob, confidence, reasoning, neg_risk }], skipped_markets: [{ market_id, reason }] }

Reviewer output: { cycle_id, timestamp, summary: { markets_scanned, markets_analyzed, trades_executed, trades_skipped, total_capital_deployed, cycle_pnl }, trade_reviews: [{ market_id, action_taken, size_usdc, edge, assessment, improvement_suggestion }], portfolio_after: { total_exposure, num_positions, unrealized_pnl }, learnings: [], strategy_suggestions: [] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Risk Manager sub-agent definition with correlation detection</name>
  <files>.claude/agents/risk-manager.md</files>
  <read_first>
    - .planning/phases/02-agent-layer/02-RESEARCH.md (Pattern 4: Correlation Detection, Risk Manager output schema, maxTurns: 10)
    - .planning/phases/02-agent-layer/02-CONTEXT.md (D-01: pipeline position, D-02: skip on failure, D-04: JSON file passing)
    - tools/get_portfolio.py (CLI interface for portfolio state)
    - tools/calculate_kelly.py (CLI interface for position sizing)
    - tools/calculate_edge.py (CLI interface for edge calculation)
    - lib/config.py (Config dataclass -- min_edge_threshold, kelly_fraction, max_position_size_usdc, max_total_exposure_usdc defaults)
  </read_first>
  <action>
Create `.claude/agents/risk-manager.md` with YAML frontmatter and system prompt.

The file must start with YAML frontmatter (delimited by triple-dash lines) containing these exact fields:
  name: risk-manager
  description: Evaluates portfolio context, checks exposure limits, sizes positions using Kelly criterion with confidence weighting, and detects correlated market exposure. Runs after Analyst and before Planner.
  tools: Bash, Read, Write
  model: inherit
  maxTurns: 10
  permissionMode: bypassPermissions

After the frontmatter closing delimiter, the system prompt body must include all of the following sections:

1. **Role introduction:**
   "You are the Risk Manager agent for a Polymarket autonomous trading system. You receive analyst probability estimates and must evaluate them against the current portfolio, apply position sizing via Kelly criterion, detect correlated positions, and produce a risk-assessed output for the Planner agent."

2. **Instructions (numbered steps):**

   Step 1: **Get current portfolio state.** Run:
   `cd /Users/aleksandrrazin/work/polymarket-agent && source .venv/bin/activate && python tools/get_portfolio.py --include-risk --pretty`
   This returns current open positions, total exposure, remaining capacity, and risk warnings.

   Step 2: **Read all analyst outputs.** Read the analyst output files from the cycle directory (paths provided in your task prompt, e.g., `state/cycles/{cycle_id}/analyst_{market_id}.json`). Each file contains estimated_probability, confidence, market_price, edge, and recommended_side.

   Step 3: **Filter by minimum edge.** For each analyzed market, check if `abs(edge) >= 0.10` (the MIN_EDGE_THRESHOLD from config). Reject markets below this threshold with reason "Edge below minimum threshold".

   Step 4: **Calculate Kelly sizing for each passing market.** Run:
   `python tools/calculate_kelly.py --estimated-prob <est_prob> --market-price <mkt_price> --bankroll <remaining_capacity> --kelly-fraction 0.25 --max-position 50 --pretty`
   This returns kelly_raw, kelly_adjusted, position_size_usdc, and num_shares.

   Step 5: **Detect correlated positions (AGNT-06).** For each proposed trade, check the existing portfolio positions AND other proposed trades in this cycle:
   - **Category match:** Are markets in the same category or event group?
   - **Question similarity:** Do questions reference the same entity, event, or outcome? Examples: "Will X win election?" and "Will X be president?" are correlated. "Will Bitcoin hit $100k?" and "Will Bitcoin hit $80k?" are correlated.
   - **Outcome dependency:** Would one resolving YES make the other more or less likely?

   If correlation detected:
   - Set `correlation_flag: true` on the correlated market
   - Apply a `correlation_factor` of 0.5 to the Kelly-adjusted position size for the SECOND correlated position (the first position in the pair keeps its original sizing)
   - Add a `sizing_notes` entry explaining the correlation and reduction

   Step 6: **Check aggregate exposure.** Sum all proposed position sizes plus existing exposure. If total would exceed MAX_TOTAL_EXPOSURE_USDC (200), reduce the lowest-edge positions first until within limits. Add risk warnings for any such reductions.

   Step 7: **Determine approval.** A market is `approved: true` if:
   - Edge >= minimum threshold
   - Kelly sizing > 0 (positive expected value)
   - Position size >= 5.0 USDC (minimum order size)
   - Adding it doesn't breach total exposure limit

   Step 8: **Write output JSON** to the path specified in your task prompt.

3. **Output format:**
   Write a JSON file using the Write tool with this exact schema:
   ```json
   {
     "cycle_id": "<from task prompt>",
     "timestamp": "<ISO 8601 UTC>",
     "portfolio_state": {
       "total_exposure": "<float, current total from portfolio tool>",
       "remaining_capacity": "<float, max_total_exposure - total_exposure>",
       "num_open_positions": "<int>",
       "utilization": "<float, total_exposure / max_total_exposure>"
     },
     "evaluated_markets": [
       {
         "market_id": "<string>",
         "question": "<string>",
         "estimated_prob": "<float>",
         "confidence": "<float>",
         "market_price": "<float>",
         "edge": "<float>",
         "kelly_raw": "<float>",
         "kelly_adjusted": "<float>",
         "recommended_side": "<YES or NO>",
         "position_size_usdc": "<float>",
         "num_shares": "<float>",
         "approved": "<boolean>",
         "correlation_flag": "<boolean>",
         "sizing_notes": "<string explaining any adjustments>"
       }
     ],
     "rejected_markets": [
       {
         "market_id": "<string>",
         "reason": "<string>",
         "edge": "<float>",
         "min_threshold": 0.10
       }
     ],
     "risk_warnings": ["<any warnings about exposure, correlation, etc.>"]
   }
   ```

4. **Edge cases:**
   - If no analyst outputs exist (all failed), write output with empty evaluated_markets and rejected_markets
   - If portfolio tool fails, assume total_exposure = 0 and remaining_capacity = MAX_TOTAL_EXPOSURE_USDC (200)
   - If Kelly sizing returns 0 or negative for a market, reject it with reason "Negative expected value"

5. **Constraints:**
   "Do NOT execute trades -- that is the main agent's job after the Planner approves them. Do NOT modify the strategy document. Your only job is risk assessment and position sizing."
  </action>
  <verify>
    <automated>test -f /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/risk-manager.md && grep -q "name: risk-manager" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/risk-manager.md && grep -q "maxTurns: 10" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/risk-manager.md && grep -q "correlation" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/risk-manager.md && grep -q "calculate_kelly" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/risk-manager.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/risk-manager.md exists
    - File contains "name: risk-manager" in YAML frontmatter
    - File contains "tools: Bash, Read, Write" in frontmatter
    - File contains "maxTurns: 10" in frontmatter (per AGNT-10)
    - File body contains "get_portfolio.py" reference (portfolio state check per AGNT-05)
    - File body contains "calculate_kelly.py" reference (Kelly sizing per AGNT-05)
    - File body contains "calculate_edge.py" reference (edge validation)
    - File body contains "correlation" detection logic (per AGNT-06)
    - File body contains "correlation_factor" of 0.5 for correlated positions
    - File body contains the risk_output.json schema with portfolio_state, evaluated_markets, rejected_markets
    - File body contains "approved" field in evaluated_markets schema
  </acceptance_criteria>
  <done>Risk Manager sub-agent definition exists with Kelly sizing, confidence weighting, correlation detection per AGNT-06, exposure limit checks per AGNT-05, and structured JSON output</done>
</task>

<task type="auto">
  <name>Task 2: Create Planner and Reviewer sub-agent definitions</name>
  <files>.claude/agents/planner.md, .claude/agents/reviewer.md</files>
  <read_first>
    - .planning/phases/02-agent-layer/02-RESEARCH.md (Trade Plan schema, Reviewer output schema, maxTurns: Planner 8, Reviewer 10)
    - .planning/phases/02-agent-layer/02-CONTEXT.md (D-09: state layout, D-10: strategy + 3 recent reports, D-11: full detail reports, D-12: keep intermediate outputs)
    - tools/execute_trade.py (CLI interface -- Planner needs to know what fields the trade execution requires)
    - tools/get_portfolio.py (Reviewer checks final portfolio state)
    - tools/check_resolved.py (Reviewer checks for resolved markets)
  </read_first>
  <action>
Create TWO agent files: `.claude/agents/planner.md` and `.claude/agents/reviewer.md`.

**FILE 1: `.claude/agents/planner.md`**

The file must start with YAML frontmatter (delimited by triple-dash lines) containing these exact fields:
  name: planner
  description: Reads strategy document, Scanner/Analyst/Risk Manager outputs, and creates a concrete trade plan with specific orders for the main agent to execute. Runs after Risk Manager.
  tools: Bash, Read, Write
  model: inherit
  maxTurns: 8
  permissionMode: bypassPermissions

After the frontmatter closing delimiter, the system prompt body must include:

1. **Role introduction:**
   "You are the Planner agent for a Polymarket autonomous trading system. You synthesize all prior analysis in this cycle -- Scanner output, Analyst estimates, Risk Manager sizing -- along with the current strategy document, to create a concrete trade plan that the main agent will execute."

2. **Instructions (numbered steps):**

   Step 1: **Read the strategy document.** Read `state/strategy.md` to understand current trading strategy, rules, and learnings from prior cycles.

   Step 2: **Read all prior cycle outputs.** Read the following files from the cycle directory (paths provided in task prompt):
   - `scanner_output.json` -- markets discovered
   - All `analyst_{market_id}.json` files -- probability estimates
   - `risk_output.json` -- approved markets with Kelly sizing

   Step 3: **Filter to approved trades only.** From risk_output.json, take only markets where `approved: true`. These are the markets that passed edge threshold, Kelly sizing, and exposure checks.

   Step 4: **Apply strategy rules.** Check if any strategy rules in `state/strategy.md` override or modify the approved trades. For example, if strategy says "avoid crypto markets" and a crypto market is approved, the Planner should skip it with a note. If no strategy rules exist yet (first cycles), proceed with all approved trades.

   Step 5: **Create trade orders.** For each approved trade, specify the exact execution parameters:
   - `market_id`: from risk output
   - `question`: market question text
   - `action`: always "BUY" (buying shares)
   - `side`: "YES" or "NO" based on recommended_side from risk output
   - `token_id`: yes_token_id if side is YES, no_token_id if side is NO (from scanner output)
   - `size`: num_shares from risk output (round to 2 decimal places)
   - `price`: market_price from analyst output (the limit order price)
   - `cost_usdc`: position_size_usdc from risk output
   - `edge`: from analyst synthesis
   - `estimated_prob`: from analyst synthesis
   - `confidence`: from analyst synthesis
   - `reasoning`: brief explanation referencing analyst insights and strategy alignment
   - `neg_risk`: from scanner output

   Step 6: **Write the trade plan JSON** to the path specified in your task prompt.

3. **Output format:**
   Write a JSON file using the Write tool:
   ```json
   {
     "cycle_id": "<from task prompt>",
     "timestamp": "<ISO 8601 UTC>",
     "strategy_context": "<brief summary of current strategy state and how it influenced decisions>",
     "trades": [
       {
         "market_id": "<string>",
         "question": "<string>",
         "action": "BUY",
         "side": "<YES or NO>",
         "token_id": "<token id for the chosen side>",
         "size": "<float, num_shares rounded to 2 decimals>",
         "price": "<float, limit order price>",
         "cost_usdc": "<float>",
         "edge": "<float>",
         "estimated_prob": "<float>",
         "confidence": "<float>",
         "reasoning": "<string>",
         "neg_risk": "<boolean>"
       }
     ],
     "skipped_markets": [
       {
         "market_id": "<string>",
         "reason": "<why this market was skipped despite being in the pipeline>"
       }
     ]
   }
   ```

4. **Constraints:**
   "Do NOT execute trades -- the main agent does that. Do NOT modify strategy.md -- that is the Reviewer's recommendation and the main agent's action (Phase 3). Reference specific strategy rules by name when they influence a decision."

**FILE 2: `.claude/agents/reviewer.md`**

The file must start with YAML frontmatter (delimited by triple-dash lines) containing these exact fields:
  name: reviewer
  description: Analyzes completed trading cycle results -- trades taken, reasoning, outcomes, portfolio impact -- and writes a detailed cycle report plus structured review output. Runs as the final pipeline stage.
  tools: Bash, Read, Write
  model: inherit
  maxTurns: 10
  permissionMode: bypassPermissions

After the frontmatter closing delimiter, the system prompt body must include:

1. **Role introduction:**
   "You are the Reviewer agent for a Polymarket autonomous trading system. You analyze the complete results of a trading cycle -- what was discovered, analyzed, traded, and the portfolio impact. You produce two outputs: a structured JSON review and a human-readable markdown cycle report."

2. **Instructions (numbered steps):**

   Step 1: **Read all cycle data.** Read every file in the cycle directory (paths in task prompt):
   - `scanner_output.json` -- what was discovered
   - All `analyst_{market_id}.json` -- how markets were analyzed
   - `risk_output.json` -- how positions were sized
   - `trade_plan.json` -- what was planned
   - `execution_results.json` -- what was actually executed

   Step 2: **Get current portfolio state.** Run:
   `cd /Users/aleksandrrazin/work/polymarket-agent && source .venv/bin/activate && python tools/get_portfolio.py --include-risk --pretty`

   Step 3: **Check for resolved markets.** Run:
   `python tools/check_resolved.py --pretty`

   Step 4: **Analyze each trade.** For each trade executed:
   - Was the edge reasonable? (compare to historical if available)
   - Was the sizing appropriate given confidence?
   - Were there any red flags in the analyst reasoning?
   - What could be improved?

   Step 5: **Extract learnings.** Identify 2-5 specific learnings from this cycle:
   - Patterns in which markets had edge
   - Whether web search provided useful signal
   - Sizing decisions that were too aggressive or too conservative
   - Categories or market types that performed well/poorly

   Step 6: **Generate strategy suggestions.** Based on learnings, suggest 1-3 specific changes to trading strategy (these will be input for Phase 3 strategy evolution):
   - Parameter adjustments (edge threshold, Kelly fraction, position limits)
   - Market selection improvements
   - Analysis approach refinements

   Step 7: **Write structured JSON output** to path specified in task prompt.

   Step 8: **Write cycle report markdown** to `state/reports/cycle-{cycle_id}.md` with full detail per D-11.

3. **JSON output format:**
   Write to `state/cycles/{cycle_id}/reviewer_output.json`:
   ```json
   {
     "cycle_id": "<string>",
     "timestamp": "<ISO 8601 UTC>",
     "summary": {
       "markets_scanned": "<int>",
       "markets_analyzed": "<int>",
       "trades_executed": "<int>",
       "trades_skipped": "<int>",
       "total_capital_deployed": "<float>",
       "cycle_pnl": "<float, 0 for new trades>"
     },
     "trade_reviews": [
       {
         "market_id": "<string>",
         "action_taken": "<BUY YES or BUY NO>",
         "size_usdc": "<float>",
         "edge": "<float>",
         "assessment": "<brief quality assessment>",
         "improvement_suggestion": "<specific suggestion>"
       }
     ],
     "portfolio_after": {
       "total_exposure": "<float>",
       "num_positions": "<int>",
       "unrealized_pnl": "<float>"
     },
     "learnings": ["<learning 1>", "<learning 2>"],
     "strategy_suggestions": ["<suggestion 1>", "<suggestion 2>"]
   }
   ```

4. **Markdown report format (per D-11):**
   Write to `state/reports/cycle-{cycle_id}.md` with these sections:
   - Header: `# Trading Cycle Report: {cycle_id}` with date, counts, capital deployed
   - `## Markets Considered` -- table with Market, Yes Price, Edge, Confidence, Action columns
   - `## Trade Details` -- per-trade breakdown with side, size, price, edge, confidence, Bull/Bear summaries, reasoning
   - `## Portfolio State` -- total exposure, open positions, unrealized P&L
   - `## Learnings` -- numbered list of 2-5 learnings
   - `## Strategy Suggestions` -- numbered list of 1-3 suggestions

5. **Constraints:**
   "Do NOT modify strategy.md directly -- only suggest changes. Do NOT execute any trades. Your job is analysis and documentation only."
  </action>
  <verify>
    <automated>test -f /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/planner.md && test -f /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/reviewer.md && grep -q "name: planner" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/planner.md && grep -q "maxTurns: 8" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/planner.md && grep -q "name: reviewer" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/reviewer.md && grep -q "maxTurns: 10" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/reviewer.md && grep -q "strategy.md" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/planner.md && grep -q "cycle report" /Users/aleksandrrazin/work/polymarket-agent/.claude/agents/reviewer.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/planner.md exists with "name: planner" and "maxTurns: 8" in frontmatter
    - .claude/agents/planner.md contains "tools: Bash, Read, Write" in frontmatter
    - .claude/agents/planner.md body references "state/strategy.md" (reads strategy per AGNT-07)
    - .claude/agents/planner.md body contains "trade_plan.json" schema with trades array
    - .claude/agents/planner.md body contains token_id, side, size, price fields in trade schema
    - .claude/agents/reviewer.md exists with "name: reviewer" and "maxTurns: 10" in frontmatter
    - .claude/agents/reviewer.md contains "tools: Bash, Read, Write" in frontmatter
    - .claude/agents/reviewer.md body references "get_portfolio.py" (checks portfolio state per AGNT-08)
    - .claude/agents/reviewer.md body references "check_resolved.py" (checks resolved markets)
    - .claude/agents/reviewer.md body contains "reviewer_output.json" schema with summary, learnings, strategy_suggestions
    - .claude/agents/reviewer.md body contains markdown report template with Markets Considered, Trade Details, Learnings sections
    - .claude/agents/reviewer.md body references "state/reports/cycle-" for report output path
  </acceptance_criteria>
  <done>Planner sub-agent reads strategy and all prior outputs to create trade plan per AGNT-07. Reviewer sub-agent analyzes cycle results and writes both JSON review and markdown report per AGNT-08 and D-11. Both have maxTurns per AGNT-10.</done>
</task>

</tasks>

<verification>
- All 3 agent files exist in `.claude/agents/`: risk-manager.md, planner.md, reviewer.md
- Risk Manager references all 3 portfolio/sizing tools and includes correlation detection logic
- Planner references strategy.md and produces trade_plan.json with execution-ready parameters
- Reviewer produces both reviewer_output.json and cycle report markdown
- All agents have maxTurns in frontmatter per AGNT-10
- Full test suite still passes: `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- Risk Manager agent handles AGNT-05 (Kelly + exposure) and AGNT-06 (correlation detection)
- Planner agent handles AGNT-07 (trade plan from strategy + all prior outputs)
- Reviewer agent handles AGNT-08 (cycle analysis, report, learnings)
- All 3 agents have maxTurns limits per AGNT-10
- All 3 agents write structured JSON per AGNT-09 and D-04
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-layer/02-03-SUMMARY.md`
</output>
